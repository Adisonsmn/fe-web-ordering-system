import type { FC } from 'react';

const LoginPage: FC = () => {
  return (
    <div className="min-h-screen flex flex-col justify-center items-center bg-off-white text-slate-dark px-6">
      <h1 className="text-[22px] font-serif font-bold mb-4">Halaman Masuk (Login)</h1>
      <p className="text-[14px] text-center text-slate-dark/60">
        Halaman masuk ini sedang dikembangkan dan akan segera siap digunakan.
      </p>
    </div>
  );
};

export default LoginPage;
